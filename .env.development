# MemeCrash development configuration for local machines running against Solana devnet.
# Copy this file into your project root and adjust any values specific to your setup.
# Secrets such as Supabase keys should only be used locally and never committed.

# Fastify API port. Override if you want the API to listen on a different port.
PORT=4000

# Primary Solana RPC endpoint used by the API, ingestor, and SDK during development.
# The default points to Solana's devnet cluster.
RPC_URL=https://api.devnet.solana.com

# Optional fallback RPC used when the primary endpoint is unavailable.
RPC_URL_FALLBACK=

# Public RPC consumed directly in the web client. Leaving this aligned with RPC_URL keeps
# the browser connected to the same cluster as the backend.
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com

# Switchboard VRF configuration is unused in SIM price mode. Leave empty unless you are
# testing VRF locally with a funded queue and keypair JSON export from Switchboard.
SWITCHBOARD_VRF_QUEUE=
SWITCHBOARD_VRF_KEYPAIR_JSON=

# Local Postgres connection string for Prisma migrations and API access.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/memecrash

# Local Supabase emulator or hosted project URL. Keep the service role key private even
# in development because it allows privileged Supabase operations.
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=

# Redis instance for BullMQ queues and caching. The defaults point to a locally hosted Redis.
REDIS_URL=redis://localhost:6379

# Admin bearer token used to protect privileged API routes (e.g. manual round settlement).
# The matching NEXT_PUBLIC_ADMIN_TOKEN below lets the admin dashboard access those routes.
ADMIN_BEARER_TOKEN=dev-admin
NEXT_PUBLIC_ADMIN_TOKEN=dev-admin

# Price engine mode. SIM streams mocked prices from the ingestor, LIVE connects to real
# on-chain markets. Development defaults to SIM so you do not need live market data.
PRICE_MODE=SIM

# Mint address for the PLAY token that fuels free-to-play rounds. Generate a devnet SPL token
# that you control (e.g. via `spl-token create-token`) and paste the mint address here.
PLAY_MINT=PLAY111111111111111111111111111111111111

# Optional override for the house token mint. Leave empty to default to USDC.
HOUSE_MINT=

# Comma separated ISO country codes to block from gameplay. The default blocks nothing.
GEO_BLOCK_LIST=

# External APIs optionally used for price discovery and liquidity snapshots.
ALCHEMY_OR_HELIUS_API_KEY=
DEX_API_KEYS_JSON={"birdeye":"","dexscreener":""}

# Internal service URLs exposed to the browser. These should point at your local API instance.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000/round

# Server-side API URL override. Useful when deploying only the ingestor or scripts locally.
API_URL=http://localhost:4000

# Optional WebSocket endpoint override for server-side consumers.
WS_URL=ws://localhost:4000/round
