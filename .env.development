# MemeCrash development environment configuration
# These defaults target a local setup on Solana devnet with mocked pricing data.
# Update the values as needed for your workstation, but avoid committing real secrets.

# --- Solana RPC endpoints ----------------------------------------------------
# Primary connection for program interactions.
RPC_URL=https://api.devnet.solana.com
# Optional fallback RPC provider. Leave blank to disable automatic fallback.
RPC_URL_FALLBACK=
# Next.js clients connect to this RPC endpoint when fetching chain data.
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com

# --- Application endpoints ---------------------------------------------------
# Base URL for the API in development mode.
NEXT_PUBLIC_API_URL=http://localhost:4000
# WebSocket endpoint that streams live round updates.
NEXT_PUBLIC_WS_URL=ws://localhost:4000/round

# --- Pricing and simulation mode --------------------------------------------
# SIM uses mocked price feeds, LIVE queries real DEX data.
PRICE_MODE=SIM

# --- Database & Supabase -----------------------------------------------------
# Supabase REST endpoint and service key for local development (replace with your own).
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=
# PostgreSQL connection string for the game database.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/memecrash

# --- Redis / Queue -----------------------------------------------------------
# Redis instance used for BullMQ queues and caching.
REDIS_URL=redis://localhost:6379

# --- Game token configuration ------------------------------------------------
# PLAY mint used for wagers during development (replace with a devnet mint you control).
PLAY_MINT=PLAY111111111111111111111111111111111111
# Optional token that represents the house treasury. Leave blank to default to USDC.
HOUSE_MINT=

# --- Switchboard VRF ---------------------------------------------------------
# Queue address and keypair JSON for randomness. These are optional in simulation mode.
SWITCHBOARD_VRF_QUEUE=
SWITCHBOARD_VRF_KEYPAIR_JSON=

# --- External APIs -----------------------------------------------------------
# Shared API key for Alchemy or Helius when fetching price data.
ALCHEMY_OR_HELIUS_API_KEY=
# JSON map of DEX API keys (e.g. {"birdeye":"", "dexscreener":""}).
DEX_API_KEYS_JSON={"birdeye":"","dexscreener":""}

# --- Compliance --------------------------------------------------------------
# Comma separated ISO country codes to block.
GEO_BLOCK_LIST=

# --- Administration ----------------------------------------------------------
# Bearer token required for server-administered routes.
ADMIN_BEARER_TOKEN=dev-admin
# Token exposed to the client for accessing protected admin pages.
NEXT_PUBLIC_ADMIN_TOKEN=dev-admin

# --- Logging -----------------------------------------------------------------
# Minimum log level for services started via pnpm dev.
LOG_LEVEL=info
