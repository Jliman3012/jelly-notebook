# MemeCrash production configuration for Solana mainnet-beta deployments.
# Populate these values through your hosting provider's secret manager. This file serves
# as documentation for required keys and should not include real secrets in version control.

# NODE_ENV should be set to production in your runtime environment.
NODE_ENV=production

# Public-facing API origin served to the Next.js frontend and other clients. Do not expose
# NEXT_PUBLIC_ADMIN_TOKEN in production so the browser admin console stays disabled.
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_WS_URL=wss://api.example.com/round
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# Fastify API port. Most managed platforms expose PORT automatically; leave blank to inherit.
PORT=

# Solana RPC endpoints for backend services. Mainnet-beta is required in LIVE mode.
RPC_URL=https://api.mainnet-beta.solana.com
RPC_URL_FALLBACK=

# Price engine mode. LIVE pulls on-chain pricing and requires funded VRF + liquidity sources.
PRICE_MODE=LIVE

# Production PLAY token mint. Provide the SPL mint address you control for faucet rounds.
PLAY_MINT=

# Optional override for the house mint used to settle wagers. Leave blank to use USDC.
HOUSE_MINT=

# Switchboard VRF configuration. Store the queue public key and encrypted keypair JSON safely.
SWITCHBOARD_VRF_QUEUE=
SWITCHBOARD_VRF_KEYPAIR_JSON=

# Postgres connection string for the primary database.
DATABASE_URL=

# Supabase project URL and service role key (keep secret!).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Redis instance for BullMQ queues, ideally a managed provider such as Upstash or ElastiCache.
REDIS_URL=

# Admin bearer token protecting privileged API routes. Keep this secret and rotate regularly.
ADMIN_BEARER_TOKEN=

# Geo compliance list of ISO country codes that should not access the game.
GEO_BLOCK_LIST=

# External API credentials. Supply production keys for Helius/Alchemy and DEX data vendors.
ALCHEMY_OR_HELIUS_API_KEY=
DEX_API_KEYS_JSON=

# Optional overrides used by server-side scripts when calling the API internally.
API_URL=https://api.example.com
WS_URL=wss://api.example.com/round
