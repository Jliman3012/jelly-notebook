# MemeCrash production environment configuration
# Populate this file with deployment credentials and secrets via your hosting platform.
# Never commit real keys to source control.

# --- Solana RPC endpoints ----------------------------------------------------
# Mainnet RPC endpoint used by back-end services.
RPC_URL=https://api.mainnet-beta.solana.com
# Optional fallback RPC provider for redundancy.
RPC_URL_FALLBACK=
# Public RPC endpoint surfaced to the Next.js client.
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# --- Application endpoints ---------------------------------------------------
# External URL for the API service.
NEXT_PUBLIC_API_URL=
# WebSocket endpoint for live round updates.
NEXT_PUBLIC_WS_URL=

# --- Pricing mode ------------------------------------------------------------
# LIVE enables real market data. Do not change to SIM in production.
PRICE_MODE=LIVE

# --- Database & Supabase -----------------------------------------------------
# Hosted Supabase project URL and service-role key.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Managed PostgreSQL connection string for production data.
DATABASE_URL=

# --- Redis / Queue -----------------------------------------------------------
# Managed Redis instance for BullMQ and caching.
REDIS_URL=

# --- Game token configuration ------------------------------------------------
# Production PLAY mint and optional treasury mint.
PLAY_MINT=
HOUSE_MINT=

# --- Switchboard VRF ---------------------------------------------------------
# Queue address and JSON keypair used for randomness proofs.
SWITCHBOARD_VRF_QUEUE=
SWITCHBOARD_VRF_KEYPAIR_JSON=

# --- External APIs -----------------------------------------------------------
# API key shared across Alchemy or Helius integrations.
ALCHEMY_OR_HELIUS_API_KEY=
# JSON blob of partner DEX API keys (e.g. {"birdeye":""}).
DEX_API_KEYS_JSON=

# --- Compliance --------------------------------------------------------------
# List of ISO country codes blocked from participating.
GEO_BLOCK_LIST=

# --- Administration ----------------------------------------------------------
# Bearer token for privileged API routes (keep secret).
ADMIN_BEARER_TOKEN=
# Do not expose an admin token publicly in production.
NEXT_PUBLIC_ADMIN_TOKEN=

# --- Logging -----------------------------------------------------------------
# Minimum log level for production observability.
LOG_LEVEL=info
